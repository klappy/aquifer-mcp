{
  "name": "aquifer-mcp",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node -e \"const p=require('./package.json'); require('fs').writeFileSync('src/version.ts', '// Generated by prebuild script — do not edit manually.\\n// Single source of truth: package.json → version.ts → everywhere.\\nexport const VERSION = \\\"'+p.version+'\\\";\\n')\"",
    "dev": "wrangler dev",
    "build": "npm run prebuild && wrangler deploy --dry-run",
    "deploy": "npm run prebuild && wrangler deploy",
    "deploy:production": "npm run prebuild && wrangler deploy",
    "deploy:staging": "npm run prebuild && wrangler deploy --env staging",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {}
}
